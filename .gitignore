build/
firmware/
managed_components/
dependencies.lock
# NVS dumps contain HomeKit pairing keys - never commit these.
backup/
sdkconfig
sdkconfig.old
platformio.ini
1
**/.DS_Store
# Vendor demo archives - large binaries do not belong in git history.
*.zip
